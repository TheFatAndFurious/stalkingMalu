# device-contract

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build device-contract` to build the library.

## Running unit tests

Run `nx test device-contract` to execute the unit tests via [Vitest](https://vitest.dev/).
